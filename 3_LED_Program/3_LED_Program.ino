/*Learning more about digitalRead() and digitalWrite() functions.This sketch causes 3 LEDs to blink depending on whether a button is pressed. */int switchState = 0;//note that this is not a const int, bc we will be changing the val/*initializes our digital pins to be INPUT or OUTPUT*/void setup() {    pinMode(3, OUTPUT);    pinMode(4, OUTPUT);    pinMode(5, OUTPUT);    pinMode(2, INPUT); //pushbutton}void loop() {    switchState = digitalRead(2);    //digitalRead() takes an int input as an ARGUMENT.    // this int corresponds to a pin.    // it checks the voltage level of the chosen pin, and stores that value in the var switchState.    // --> after executing this command, switchState will be either HIGH (1), if there is voltage,    // or LOW (0), if there is no voltage on the pin.        if (switchState == LOW){ //a conditional statement testing for equality      //the button is not pressed      digitalWrite(3, HIGH); //green LED      digitalWrite(4, LOW); //red LED      digitalWrite(5, LOW); //red LED    }    else{ //i.e., switchState == HIGH      //the button is pressed      digitalWrite(3, LOW);      digitalWrite(4, LOW);      digitalWrite(5, HIGH);            delay(250); //wait for a quarter second      digitalWrite(4, HIGH);      digitalWrite(5, LOW);      delay(250);    }       }// this end-bracket closes the loop() function